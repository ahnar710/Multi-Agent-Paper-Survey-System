# data 文件夹：必须保留的运行状态

这里保存 SQLite 数据库、LangGraph checkpoint、上传论文和最终报告。Docker 会把
本目录挂载到容器的 `/app/data`，因此重建容器后状态仍然存在。

迁移历史任务到另一台电脑时，应在程序停止后复制整个 `data` 文件夹。制作通用
交付包时不包含个人运行数据，只保留本说明；API Key 也永远不应放在这里。

